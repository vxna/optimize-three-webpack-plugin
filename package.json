{
  "name": "@vxna/optimize-three-webpack-plugin",
  "version": "0.0.0-development",
  "description": "A compat layer that enables tree shaking and human-readable imports",
  "author": "vxna",
  "license": "MIT",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/vxna/optimize-three-webpack-plugin.git"
  },
  "keywords": [
    "webpack",
    "plugin",
    "three"
  ],
  "engines": {
    "node": ">= 8"
  },
  "scripts": {
    "aliases": "node utils/generate",
    "pretest": "yarn lint && yarn aliases",
    "test": "yarn test:jest",
    "test:jest": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "posttest": "yarn format",
    "lint": "eslint . --cache --fix",
    "format": "prettier --write **/*.{js,md}"
  },
  "devDependencies": {
    "eslint": "^6.5.0",
    "eslint-config-tamia": "^7.0.1",
    "file-loader": "^4.2.0",
    "husky": "^3.0.7",
    "jest": "^24.9.0",
    "lint-staged": "^9.4.0",
    "listdirs": "^3.1.1",
    "memory-fs": "^0.4.1",
    "prettier": "^1.18.2",
    "three": "^0.109.0",
    "webpack": "^4.41.0"
  },
  "eslintConfig": {
    "extends": "tamia"
  },
  "publishConfig": {
    "access": "public"
  },
  "prettier": {
    "singleQuote": true,
    "semi": false
  },
  "jest": {
    "testEnvironment": "node"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write",
      "git add"
    ],
    "*.md": [
      "prettier --write",
      "git add"
    ]
  }
}
